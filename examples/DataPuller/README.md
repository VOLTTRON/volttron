## Data Puller Example Agent

This agent is a historian that pull data from another platform.

To install the agent, run this command:
```
vctl install --agent-config examples/DataPuller/config examples/DataPuller
```