[control]
socket = string(default=@volttron-platform-control-socket, min=1, max=107)
allow-root = boolean(default=True)
users = force_list(default=list())
groups = force_list(default=list())

[agent-exchange]
publish-address = string(default=ipc:///tmp/volttron-platform-agent-publish)
subscribe-address = string(default=ipc:///tmp/volttron-platform-agent-subscribe)
append-pid = boolean(default=False)

[agent-paths]
config-dir = string(default=~/.config/volttron/platform)
bin-dir = string(default=~/.local/share/volttron/platform/bin)
run-dir = string(default=/tmp)
