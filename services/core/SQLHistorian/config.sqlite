{
    "connection": {
        "type": "sqlite",
        "params": {
            # if no directory is given location will be under install_dir/<agent name>.agent-data directory
            # In secure mode this will be only directory in which agent will have write access
            "database": "historian_test.sqlite"
        }
    },
    "all_platforms":true
}
